（1）编辑对话框设置为卡片Panel，以备空间给新属性
	Note新属性：类型（可自定义，或许得和纸张颜色绑定）、完成状态、起止时间
		起止时间的设置可以不是日历选择，而是由完成状态设置
（2）Note可能需要id作为识别编号，重写equals方法，TaskQuadrant内部设置计数属性（需存储）
（3）数据存储：日期，该日期的Note列表
	如果有多个日期——一个总列表，根据起止日期分配Note到各个列表
	同一天存储，新文件覆盖旧文件
（4）档案文件夹：存储不同日期的归档
	不同档案文件夹可以有相同日期的不同归档

改（3）不再按日期保存，而是另一种方式：分全局模式和局部模式，局部是全局的一个视图，
	用户可以新建视图，将需要的加入，局部模式中可以按按钮查看不同视图

改Note颜色为新类：CategoryLabel，成员包括颜色、分类名。
管理：分两路，（1）在Note中用JComboBox选择，最后一个选项是新建，点击打开新建窗口
（2）在管理当前所有CategoryLabel的窗口（通过菜单栏打开），每个对象以一个panel显示：颜色键、文本域、确认修改文本键、取消键、删除键；
默认存在一个初始的分类：个人，紫色
